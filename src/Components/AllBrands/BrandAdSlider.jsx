import React from 'react';

const BrandAdSlider = ({ advertisementProduct }) => {
    advertisementProduct.map((pro, ind) => console.log(ind, ind + advertisementProduct.length))
    return (
        <div>

        </div>
    );
};

export default BrandAdSlider;